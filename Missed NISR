STR(
  ROUND(
    SUM(IF [MetMissed] = "Missed" THEN 1 ELSE 0 END) * 100.0
    /
    (SUM(IF [MetMissed] = "Missed" THEN 1 ELSE 0 END) + SUM(IF MetMissed = "Met" THEN 1 ELSE 0 END)),
    2
  )
) + "%          " +
STR(SUM(IF MetMissed = "Missed" THEN 1 ELSE 0 END))
